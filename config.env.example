# Example configuration for Hardware Inventory System
# Copy to config.env and modify as needed

# Web interface settings
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=False

# Database location (default: data/hardware_inventory.db)
# INVENTORY_DB=/var/lib/hardware-inventory/inventory.db

# Service settings (for systemd installation)
SERVICE_USER=www-data
SERVICE_GROUP=www-data
INSTALL_PATH=/opt/hardware-inventory
